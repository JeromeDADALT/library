{% extends 'common/base.html.twig' %}

{% block bookContent %}
    <main class="milieu">
        <div>
            <h1 class="titre">{{ book.title }}</h1>
            <h2 class="soustitre">{{ book.author }}</h2>
            <P>Résumé : {{ book.resume }}</P>
            <p>Nombre de pages : {{ book.nbPages }}</p>
            <h3>
                <a href="{{ path('books_list') }}">Retour à l'accueil</a>
            </h3>
        </div>
    </main>
{% endblock %}